#include <iostream>
using namespace std;

int main() 
{
//    int a = 5, b = 7;
//    int c = a + b;

    // float a = 5.8, b = 7.6;
    // double c = a - b;

    // int d = (int)(a - b);
    // int d2 = (int)a - b;

    // P=2*(a+b)
    // double a1, b1;
    // cin >> a >> b;

    // double p = 2*(a+b);
    // cout << "p= " << p << endl;

    
    int a2 = 7;
    int b2 = 2;

    double c2 = a2 / (double)b2;
    float d3 = 7.0 / 2;

    short var = 0;
    var ++;

   cout << var << endl;
   return 0;
}